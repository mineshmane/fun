import { FilternotePipe } from './filternote.pipe';

describe('FilternotePipe', () => {
  it('create an instance', () => {
    const pipe = new FilternotePipe();
    expect(pipe).toBeTruthy();
  });
});
